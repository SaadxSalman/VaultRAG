[package]
name = "pharma_sim"
version = "0.1.0"
edition = "2021"

[lib]
name = "pharma_sim"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
# Add rdkit or chemistry crates here if needed