[project]
name = "vaultrag"
version = "0.1.0"
description = "Local, privacy-preserving federated RAG workspace"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
